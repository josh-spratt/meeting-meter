use anyhow::Result;

pub fn run(limit: usize) -> Result<()> {
    // TODO: Implement history viewing in Phase 4
    println!(
        "Meeting history (last {} meetings) - coming soon in Phase 4!",
        limit
    );

    Ok(())
}
